---
id: two-state-candidate-decision-not-tracking
type: lesson
status: validated
scope: global
domain: radar-signal
tags: [候选判决, 两态跳变, 跟踪器, argmax, 水面回波]
triggers:
  - "单帧输出在两个稳定值（如水面/结构）之间跳变，第一反应想上 α-β/门控/中值滤波平滑"
  - "多候选按最大 SNR / argmax 每帧直接输出，事后统计发现稳定锁在错误候选（失败信号）"
  - "跟踪器输出看起来很平稳（切换/天 低），但目标身份是错的（失败信号）"
  - "两候选幅度接近，单帧选择像抛硬币，需要判断该建成跟踪问题还是概率判决"
  - "想把『抖动脉冲』交给跟踪滤波解决前，要量化平滑到底能挽回多少身份正确率"
created: 2026-09-21
evidence: {helpful: 0, harmful: 0}
verified_by: command
source: session:01a0bdef-64b5-738d-af4a-290ada962882
last_verified: 2026-09-21
superseded_by: null
schema_version: 1
related: [multi-seed-identical-readout-suspect-seed-not-wired, track-gap-attribution-viewport-gate-vs-filter]
---

# 双态候选跳变先做身份概率判决，α-β/门控平滑救不回来

## 主张

上游每帧在两候选间做硬选择（「取最大 SNR/argmax」直接输出）时，若候选近似双态（如 WTR10 的水面/结构），下游再加 α-β + 门控平滑也无法纠正身份误选：实测门控 α-β 落在正确候选（水面）的比例只有 0.3%，与基线直通 0.0% 几乎相同；正确做法是把问题建成候选身份的概率判决（用散斑统计/相干性等），跟踪只是判决之后的从属环节。

## 证据（session 01a0bdef，wtr10/dsp_research 仿真脚本）

- 上游标定：两态占比约 30%（水面 29.6%，报告实测 29%）；「每帧取 SNR 最大者直接输出」落在水面 0.4%、RMS 7.716 m、切换/天 888。
- 平滑极限（无净化两态输入）：基线直通落在水面 0.0%、RMS 7.730 m、切换/天 1；加门控 α-β（无 SNR 判据）后落在水面仅 0.3%。另一组参数的 argmax 上游水面占比 14.4%、基线 RMS 7.153 m、切换/天 21933。
- 会话收尾结论（末条 assistant）：「这是概率判决（candidate identity decision），不是跟踪。跟踪（α-β 平滑轨迹）只是判决之后的从属环节。」

## 边界 / 反例

- 两条失败信号性质不同：argmax 版切换/天数千、抖动明显；门控 α-β 版「切换/天 1」看起来很平稳，但那是锁死在错误候选上的假稳定——平稳不等于判对。
- 以上均为按报告实测统计标定的合成两态模型（散斑统计 + 固件定义）仿真，不是外场复核；真实场景的判决统计量（散斑/相干性）需另行验证。
- 若上游候选本身可分（不是两态近似），跟踪平滑仍是正常手段；本条只针对「单帧判决本身错误、跟踪被当成长矛」的情形。

## 2026-09-22 独立复核增补

下列是复核时在本机跑过的**自包含最小复现**：

```
python3 - <<'PY'
import numpy as np
rng=np.random.default_rng(7); R_S,R_W=6.69,14.40; FS=2.0; N=int(FS*86400*2)
is_w=rng.random(N)<0.30                      # 真值水面占比 30%
z=np.where(is_w&(rng.random(N)<0.014),R_W,R_S)  # 上游「取 SNR 最大者直接输出」
def pd(c): return c/(N/FS/86400)
print("基线(上游直接输出)  落在水面 %.1f%%  切换/天 %.0f"%(100*(z==R_W).mean(), pd((np.diff(z)!=0).sum())))
x,v=R_S,0.0; land=0                          # 下游：门控 α-β（门内更新，门外拒绝）
for k in range(N):
    p=x+v; r=z[k]-p
    if abs(r)<=3.0: x=p+0.5*r; v=v+0.1*r
    if abs(x-R_W)<abs(x-R_S): land+=1
print("+门控 α-β(无 SNR 判据) 落在水面 %.1f%%"%(100*land/N))
PY
# 期望输出：
# 基线(上游直接输出)  落在水面 0.4%  切换/天 1464
# +门控 α-β(无 SNR 判据) 落在水面 0.0%
# 说明：上游硬选择丢掉了身份，门控 α-β 把少数派（真水面）当离群点拒掉、锁死在结构候选上，落在水面的比例仍≈0——与切片第 239/241 行同构。
```


**审核给出的修改意见（要点）**：只改「边界 / 反例」节第 1 条：把「门控 α-β 版『切换/天 1』看起来很平稳」改为「基线（上游直接输出）版『切换/天 1』看起来很平稳」，并删掉把该数字同时归到门控 α-β 的说法（与证据节自述一致；切片第 239 行该数字在基线行，门控 α-β 行的切换数被截断）。可选：在证据节注明三个数字来自 wtr10/dsp_research 的合成模型仿真，该仓 dsp_research/ 已在其他会话删除、不可重跑——真正的可复现证据改由最小复现脚本承担。

**复核指出、尚未逐条改写进正文的断言**（读正文时以本节为准）：
- 「门控 α-β 版『切换/天 1』看起来很平稳，但那是锁死在错误候选上的假稳定」——切片第 239 行把「切换/天 1」记在「基线(现状:直接输出)」行，门控 α-β 行的切换/天被截断；此归因与切片不符，且与本条证据节自述相冲突。定性（门控 α-β 锁死在错误候选）大致成立，但所引数字点错了对象。

**判定**：keep-with-fix · 拟 promote-playbook · 原证据快照风险=low · 复核时本机可复跑=true
